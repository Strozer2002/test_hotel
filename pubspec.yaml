name: test_hotel
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  reactive_variables:
    version: ^0.0.1
    git:
      url: git@github.com:dronn1k-org/reactive_variables.git
      ref: master
  base_widget_controller:
    version: ^0.0.1
    git:
      url: git@github.com:dronn1k-org/base_widget_controller.git
      ref: master
  data_source:
    version: ^0.1.1
    git:
      url: git@github.com:dronn1k-org/data_source.git
      ref: master

  cupertino_icons: ^1.0.2
  go_router: ^10.0.0
  logger: ^2.0.1
  easy_localization: ^3.0.1
  flutter_svg: ^2.0.1
  get_it: ^7.2.0
  json_annotation: ^4.8.1
  retrofit: ^4.0.1
  dio: ^5.0.0
  carousel_slider: ^4.2.1
  intl: ^0.18.0
  flutter_masked_text2: ^0.9.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.6.1
  retrofit_generator: ^7.0.0
  build_runner: ^2.3.3
  isar_generator: ^3.1.0+1


  flutter_lints: ^2.0.1

flutter:


  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
   

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: SFProDisplay
  #     fonts:
  #       - asset: assets/fonts/SFPRODISPLAY/SFPRODISPLAYREGULAR.OTF
  #       - asset: assets/fonts/SFPRODISPLAY/SFPRODISPLAYBLACKITALIC.OTF
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
